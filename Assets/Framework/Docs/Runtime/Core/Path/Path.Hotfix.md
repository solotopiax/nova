# Path.Hotfix

**类签名**：`public static class Path.Hotfix`
**命名空间**：`NovaFramework.Runtime`

`Path` 静态类中的热更新相关路径分区。当前代码里只保留 APK 包下载后的本地保存路径。

---

## 文件列表

| 文件 | 说明 |
|------|------|
| `Path.Hotfix.cs` | 热更新路径常量 |

## 关键字段

| 字段 | 类型 | 说明 |
|------|------|------|
| `ApkDownloadedFullPath` | `string` | APK 包下载后的保存绝对路径（`{persistentDataPath}/update.apk`） |

## 关联文档

- [Path](Path.md)
