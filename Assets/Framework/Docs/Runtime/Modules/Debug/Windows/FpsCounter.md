# FpsCounter（已移除）

`FpsCounter` 已不在当前源码中。

它原本属于旧版 Runtime 自绘调试面板的局部计数器实现。当前 `Debug` 模块只保留 `RuntimeDebugger` 激活与磁盘检测能力，不再维护这类运行时自绘性能计数器。

## 当前应查看

- [../DebugComponent.md](../DebugComponent.md)
- [../DebugManager.md](../DebugManager.md)
