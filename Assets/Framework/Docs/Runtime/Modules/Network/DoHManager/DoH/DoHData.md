# DoHData（已移除）

`DoHData` 已不在当前源码中。

当前 `DoHManager` 的现行实现与对外事实，请直接看：

- [DoHManager.md](../DoHManager.md)
- [NetworkComponent.md](../../NetworkComponent.md)

如果你在旧资料里看到 `DoHData`，应将它视为退役内部类型，而不是当前可依赖的运行时数据结构。
