syntax = "proto3";

package NovaFramework.Runtime;

// 通用响应包装体：服务端所有接口的统一返回结构
message PbNetBaseResponse {
    int32 code = 1;      // 业务状态码，0 表示成功，非 0 表示各类业务/系统错误
    string message = 2;  // 错误描述或附加提示信息，成功时可为空
    bytes data = 3;      // 实际业务响应数据的序列化字节流，由具体业务 proto 反序列化
}
